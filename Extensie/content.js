// Exemplu de completare a unui formular de login
document.addEventListener("DOMContentLoaded", function () {
    let loginForm = document.querySelector("form");
    if (loginForm) {
        // Aici poți adăuga logica pentru completarea automată a câmpurilor de login
    }
});
